{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "composer install --no-dev --optimize-autoloader && composer dump-autoload --optimize"
  },
  "deploy": {
    "startCommand": "php artisan key:generate --force && php artisan config:cache && php artisan route:cache && exec php artisan serve --host=0.0.0.0 --port=$PORT"
  }
}
