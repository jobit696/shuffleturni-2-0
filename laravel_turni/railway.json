{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "composer install --no-dev --optimize-autoloader"
  },
  "deploy": {
    "startCommand": "ls -la public/build/assets/ && php artisan config:clear && php artisan view:clear && php artisan serve --host=0.0.0.0 --port=$PORT"
  }
}
