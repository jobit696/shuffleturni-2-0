{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "composer install --no-dev --optimize-autoloader && touch database/database.sqlite"
  },
  "deploy": {
    "startCommand": "php artisan config:clear && php artisan serve --host=0.0.0.0 --port=$PORT"
  }
}
