{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "composer install --no-dev --optimize-autoloader && mkdir -p database && touch database/database.sqlite && chmod -R 775 storage bootstrap/cache database"
  },
  "deploy": {
    "startCommand": "php artisan config:clear && ls -la database/ && exec php artisan serve --host=0.0.0.0 --port=$PORT"
  }
}
