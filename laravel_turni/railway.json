{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "composer install --no-dev --optimize-autoloader && php artisan config:cache"
  },
  "deploy": {
    "startCommand": "exec php artisan serve --host=0.0.0.0 --port=$PORT"
  }
}
