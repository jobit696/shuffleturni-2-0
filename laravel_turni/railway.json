{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "composer install --no-dev --optimize-autoloader"
  },
  "deploy": {
    "startCommand": "echo '=== CREATING DATABASE ===' && pwd && ls -la && mkdir -p database && touch database/database.sqlite && chmod 666 database/database.sqlite && ls -la database/ && echo '=== DATABASE CREATED ===' && php artisan config:clear && exec php artisan serve --host=0.0.0.0 --port=$PORT"
  }
}
